
#!/bin/bash

####################################################################################

DATE=$1
SOURCE=$2
DEST=$3

aws s3 sync "${SOURCE}/${DATE}/" "${DEST}/${DATE}" \
  --exclude '*/_ERR' \
  --exclude '*/_SUCCESS' \
  --exclude '*/_OUT' \

####################################################################################